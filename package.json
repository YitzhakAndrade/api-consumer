{
  "name": "rest-api-fetcher",
  "version": "0.1.0",
  "description": "rest-api-fetcher",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "browserify main.js | uglifyjs > bundle.js",
    "bundle-debug": "browserify main.js -o bundle.js -d",
    "bundle-watch": "watchify main.js -o bundle.js -d -v"
  },
  "author": "Yitzhak Andrade",
  "license": "ISC",
  "dependencies": {
    "jquery": "^3.3.1"
  },
  "devDependencies": {
    "browserify": "^14.4.0",
    "uglify-es": "^3.1.0",
    "watchify": "^3.9.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/YitzhakAndrade/rest-api-fetcher.git"
  }
}
